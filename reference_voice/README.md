# 参照音声フォルダ

このフォルダに **host.wav** を配置してください。

- テキストを送ったユーザーに対して、この音声の声質で読み上げた音声が返されます
- 推奨: 30秒程度のクリアな音声（モノラル、16kHz 推奨だが、他の形式も自動変換されます）
- ファイル名は **host.wav** にしてください

## 配置例

```
reference_voice/
  host.wav   ← ここにあなたの声の音声ファイルを置く
```

m4a や mp3 の場合は、事前に wav に変換してから配置するか、host.m4a も対応可能です（要コード変更）。
